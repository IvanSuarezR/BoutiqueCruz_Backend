# Django Settings
SECRET_KEY=cambia-esta-clave-secreta-por-una-muy-larga-y-segura
DEBUG=True

# Database Configuration (opcional - por defecto usa SQLite)
# Cambia a True para usar PostgreSQL
USE_POSTGRES=False
DB_NAME=boutique_db
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_HOST=localhost
DB_PORT=5432

# CORS Settings (Frontend URL)
FRONTEND_URL=http://localhost:5173

# Optional: Para producción
# ALLOWED_HOSTS=tudominio.com,www.tudominio.com
