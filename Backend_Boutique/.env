# Switch to PostgreSQL (1 to enable, 0 to use SQLite)
USE_POSTGRES=1

# PostgreSQL connection settings
POSTGRES_DB=boutique_ropa
POSTGRES_USER=postgres
POSTGRES_PASSWORD=1234
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional: keep DB connections open for reuse (seconds)
POSTGRES_CONN_MAX_AGE=60
