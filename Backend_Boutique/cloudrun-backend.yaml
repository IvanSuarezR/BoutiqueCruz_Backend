# ==========================================
# Cloud Run Configuration - Backend
# ==========================================

# Para desplegar el backend:
# gcloud run deploy boutique-backend \
#   --source ./Backend_Boutique \
#   --platform managed \
#   --region us-central1 \
#   --allow-unauthenticated \
#   --port 8080 \
#   --memory 1Gi \
#   --cpu 1 \
#   --min-instances 0 \
#   --max-instances 10 \
#   --timeout 300 \
#   --set-env-vars "DEBUG=False,USE_POSTGRES=true,USE_GCS=true" \
#   --set-secrets "SECRET_KEY=django-secret:latest,POSTGRES_PASSWORD=postgres-password:latest"

# Variables de entorno en Cloud Run Console:
# - DEBUG=False
# - SECRET_KEY=<desde Secret Manager>
# - ALLOWED_HOSTS=*.run.app
# - USE_POSTGRES=true
# - POSTGRES_DB=boutique_db
# - POSTGRES_USER=postgres
# - POSTGRES_PASSWORD=<desde Secret Manager>
# - POSTGRES_HOST=/cloudsql/PROJECT:REGION:INSTANCE
# - USE_GCS=true
# - GS_BUCKET_NAME=tu-bucket
# - GROQ_API_KEY=<desde Secret Manager>
# - STRIPE_SECRET_KEY=<desde Secret Manager>
